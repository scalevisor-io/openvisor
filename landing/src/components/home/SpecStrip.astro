---
import content from '../../lib/content';
---
<div class="spec-strip rise r6">
  <div class="container spec-inner">
    {content.specs.map((spec) => <span class="spec-item">{spec}</span>)}
  </div>
</div>
